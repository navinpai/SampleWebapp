## Simple Webapp

- This webapp runs on port `4567` and has one endpoint `http://localhost:4567/hello` which outputs hello world. The application uses jetty as a web container.
- To run locally run `gradle appRun`
- To build/assemble/war use `gradle build` or `gradle assemble` or `gradle war`

### License

Licensed under The Unlicense. Check LICENSE for detailed license